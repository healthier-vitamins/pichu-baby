const dateSchema: DateSchema = {
  TITLE: "DATE",
  STATUS: "STATUS",
  CREATED_TIME: "CREATED_TIME",
  CREATED_BY: "CREATED_BY",
};

interface DateSchema {
  TITLE: string;
  STATUS: string;
  CREATED_TIME: string;
  CREATED_BY: string;
}

export { dateSchema };
